"""Critical alert notifier worker (skeleton)."""


class TelegramNotifier:
    async def run_once(self) -> None:
        # TODO: pull new critical alerts and deliver to subscribers.
        return None
